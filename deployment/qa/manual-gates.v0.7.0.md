# Manual gates — CityChat DS Add-on Playground v0.7.0

Artifact build: `citychat-ui-20260823-02`
Scope: source-limited team-learning projection implementing CityChat DS Add-on v0.7 and inheriting the approved CityChat Visual Experience Specification v0.6 historical base.

## Closed by explicit owner direction and exact artifact records

- [x] Republish artifact v0.7.0 at the existing canonical URL after all automated/provider/live gates pass.
- [x] Use the exact unchanged horizontal CityChat PNG in separately scoped header and footer roles for this exact build and URL.
- [x] Use the LDS brand-beige identity band without a local white logo plate.
- [x] Add a deterministic two-shape LDS button guide, a governed self-hosted icon subset, scoped motion examples and a CityChat Color Usage Atlas.
- [x] Restore the place-first CityScan hero composition from the v0.4 lineage using current LDS map roles and the current truth boundary.
- [x] Publish a bounded, directly downloadable CityScan hero specimen without transferring a logo role approval.

## Place-first CityScan hero

- [x] The restored composition is traceable to commit `a0b7048`; its visual map was HTML/CSS rather than a missing raster asset.
- [x] The current hero keeps a visible place, one meaning, one limitation, one question and one labelled capsule action.
- [x] The visible fixture label says the example is synthetic and not real place data.
- [x] Map colour jobs use pinned LDS map/marker variables; the hero contains no analytical scale, score, official status or product-identity colour acting as data.
- [x] Hero motion is `none`; no sweep, pulse, loop, shimmer or parallax suggests a live scan.
- [x] The downloadable specimen links directly to the no-score CityScan example without JavaScript and intentionally omits logo artwork outside its approved canonical header/footer roles.
- [ ] Confirm the canonical hero action reaches the no-score CityScan example with JavaScript disabled; `href="#play"` alone is not sufficient evidence.
- [ ] Bind the canonical hero action and its `map` glyph in both `control-inventory.v0.7.0.json` and `citychat-icon-resolution.json` before freezing release records.
- [ ] Review the drawn boundary in Safari and Android: a clipped rectangular border must visibly read as a coherent boundary rather than broken edge fragments.
- [ ] At 320, 360 and 390 CSS px and a short 390×480 viewport, confirm the first meaning remains visible, Thai text does not collide with the map, and the fixture label stays readable.
- [ ] At light, dark and system themes, confirm the story card, question, scan frame, route lines, markers and fixture label retain their required contrast and do not imply a value scale.
- [ ] Keyboard and screen-reader review confirms the action changes the visible view, communicates the no-score state, and leaves focus/context understandable after the page moves to `#play`.
- [ ] Compare the released hero visually with the v0.4 lineage at desktop and mobile sizes: retain spatial depth and layered StoryCard composition without restoring the old `color-mix()` implementation.

## Identity surface

- [x] Header and footer use the exact PNG bytes and intrinsic ratio; no crop, recolour, filter, mask, opacity change, transform or animation.
- [x] The logo is not placed on the CityChat gradient or an invented local carrier.
- [ ] Native Safari and Android actual-size recognition and clear-space review.
- [ ] Separate approvals for compact, favicon, touch, maskable, social, share, splash, watermark, export and motif roles; all remain omitted.

## Controls, icons and motion

- [x] Every new action is a working local control, a truthful disabled specimen, or a real resource link.
- [x] Material Symbols bytes, source, axes, glyph list, license and hash are recorded; runtime CDN use is absent.
- [x] Library-only roles are visibly labelled and do not activate disabled product capabilities.
- [x] Motion uses inherited LDS `.btn` and one once-only `.reveal` reading-order group; replay and fake receipt motion are absent.
- [ ] VoiceOver, TalkBack, iOS touch press and Android touch press on physical devices.
- [ ] Real OS reduced-motion change while the example is approaching the viewport.

## Color, language and layout

- [x] CityChat Color Usage Atlas is a role overlay over `color-srgb-05`, not a second palette.
- [x] No analytical ramp is invented; no-data and zero remain distinct in text.
- [x] Frontstage Thai remains plain, short and human; team terminology stays in the implementation layer.
- [ ] Citizen and officer read-aloud/comprehension session.
- [ ] Native-browser 200% zoom, Thai expansion and landscape safe-area review.

## Release receipt

- [ ] Required protected PR check passes on the exact reviewed commit.
- [ ] GitHub Pages deploy succeeds from merged `main`.
- [ ] Exact live critical-asset and manifest parity passes.
- [ ] Immutable commit, workflow run, deployment ID, URL and live hashes are recorded.

Open manual gates do not block this source-limited team projection unless the related experience is claimed as native-device-, product-runtime-, or production-conformant.
